<p id="notice"><%= notice %></p>

<h1>Listing Vehicles</h1>

<%= will_paginate @vehicle %>

<table>
  <thead>
    <tr>
      <th>Ticker</th>
      <th>Name</th>
      <th>Currency</th>
      <th>Last price ($)</th>
      <th>Return (%)%)</th>
      <th>Risk (%)%</th>
      <th>Sharpe Ratio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @vehicle.each do |vehicle| %>
      <tr>
        <td><%= vehicle.ticker %></td>
        <td><%= vehicle.name %></td>
        <td><%= vehicle.currency %></td>
        <td><%= number_with_precision(vehicle.last_price, precision: 2) %></td>
        <td><%= number_to_percentage(vehicle.return*100, precision: 1) %></td>
        <td><%= number_to_percentage(vehicle.risk*100, precision: 1) %></td>
        <td><%= number_with_precision(vehicle.sharpe_ratio, precision: 3)   %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= will_paginate @vehicle %>
<br>

<%= link_to 'New Vehicle', new_vehicle_path %>